already tested to work on SSP.
only tested on demo example, so needs checking with other rnbo patches, ensure compatibility with rnbo.example.ssp 
note: previous rnbo patches will need reexporting with RNBO 1.4.2 using new settings (as per docs)